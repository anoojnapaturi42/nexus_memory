import { FeaturePlaceholder } from "@/components/layout/feature-placeholder";

export default function SettingsPage() {
  return (
    <FeaturePlaceholder
      title="Settings"
      description="Configure preferences, agent behavior, sync settings, and workspace policy."
    />
  );
}
